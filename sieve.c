#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int sieve(int target){
  //int  n[] = malloc((int)(target*log(target)*1.15)); //error
  char numbers[1000];
  numbers[0] = 1;
  numbers[1] = 1;
  numbers[2] = 1;
  for(int x = 4; x < size; x+=2){
    numbers[x] = 1;
  }

  for ()
  return 0;
}
